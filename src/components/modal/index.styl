.top-modal {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    display: none;
    background-color: rgba(0, 0, 0, 0.6);
    z-index: 1000;
}

.top-modal.show {
    display: block;
    bottom: 0;
}

.top-modal.show >.top-dialog {
    width: 100%;
    background-color: white
}