首页
<common-grid   title="这是示例" items="{{items}}"></common-grid>

<common-grid title="这是权限控制示例" items="{{items}}" access="{{access}}" accessControl="{{true}}"></common-grid>

<common-grid title="显示控制" items="{{[{accessId:'no'}]}}" access="{{access}}" accessControl="{{true}}"></common-grid>